import { AppShell } from '@/components/layout/AppShell';
import { TelemetryReports } from '@/components/telemetry-platform/TelemetryReports';

export default function TelemetryReportsPage() {
  return (
    <AppShell>
      <TelemetryReports />
    </AppShell>
  );
}
