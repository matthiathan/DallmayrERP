import { TelemetryIntervalSettings } from '@/components/features/TelemetryIntervalSettings';
import { AppShell } from '@/components/layout/AppShell';

export default function TelemetryIntervalsPage() {
  return (
    <AppShell>
      <TelemetryIntervalSettings />
    </AppShell>
  );
}
